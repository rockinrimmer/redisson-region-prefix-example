To run have a Redis server running on localhost:6379

After start up run `keys *` on the redis server and you will see the following:

![](redis-keys.png)

`redisson-hibernate-timestamp` is not prefixed
